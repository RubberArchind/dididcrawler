services:
  app:
    build: .
    container_name: dididcrawler-app
    ports:
      - "8080:80"
    environment:
      - APP_ENV=production
      - APP_DEBUG=false
      - APP_KEY=base64:JIan8waFhv4Bt4U3e4+hzxNiHE4QW9ty3feqsHR/n1U=
      - APP_URL=http://localhost:8080
      - DB_CONNECTION=mysql
      - DB_HOST=host.docker.internal  # For testing with local MySQL
      - DB_PORT=3306
      - DB_DATABASE=dididcrawler
      - DB_USERNAME=root
      - DB_PASSWORD=AlphA2112
      - SEED_DATABASE=false  # Set to true only for initial setup
    volumes:
      - ./storage:/var/www/html/storage
    restart: unless-stopped

  # Optional: Local MySQL for testing (comment out if using external DB)
  # mysql:
  #   image: mysql:8.0
  #   container_name: dididcrawler-mysql-local
  #   restart: unless-stopped
  #   environment:
  #     MYSQL_DATABASE: dididcrawler
  #     MYSQL_ALLOW_EMPTY_PASSWORD: "yes"
  #     MYSQL_ROOT_PASSWORD: ""
  #   volumes:
  #     - mysql_data:/var/lib/mysql
  #   ports:
  #     - "3306:3306"
  #   command: --default-authentication-plugin=mysql_native_password

# volumes:
#   mysql_data: